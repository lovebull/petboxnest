检查storefront项目，看下还有什么欠缺，需要增加的地方，还有什么未完善的地方。请给出结果，先不修改代码。





## 审计结论

Storefront 的 PetBoxNest 视觉体系已经基本成形，首页、商品页、购物车和内容页的响应式布局整体可用，TypeScript 检查通过。

但目前还不适合直接作为完整正式商城上线。主要短板不是页面外观，而是若干“看起来可以使用、实际上没有完成”的功能，以及性能、SEO、商品内容和测试保障不足。

本次仅检查，没有修改代码。

## 最高优先级问题

### 1. 账户邮箱和密码修改是假功能 【已经修复】

这是目前最严重的问题。

- 邮箱修改没有调用后端，却直接返回成功。
- 密码修改只输出一条控制台信息，没有真正更新密码。
- 用户可能误以为账户资料已经修改成功。

证据：

- [profile-email/index.tsx](/www/wwwroot/petboxnest/apps/storefront/src/modules/account/components/profile-email/index.tsx:18)
- [profile-password/index.tsx](/www/wwwroot/petboxnest/apps/storefront/src/modules/account/components/profile-password/index.tsx:16)

建议：在实现真正的安全更新流程前，隐藏或禁用这两个编辑入口，不能继续显示虚假成功状态。

### 2. 首页邮件订阅表单没有真正提交  【已经修复】

表单当前只有：

```tsx
action="#early-access"
```

提交后只是跳回当前锚点，没有保存邮箱、没有成功提示，也没有防重复或错误处理。

证据：[early-access-signup/index.tsx](/www/wwwroot/petboxnest/apps/storefront/src/modules/home/components/early-access-signup/index.tsx:17)

建议：连接 Payload CMS、Resend 或专门的订阅表，并加入确认状态、错误状态、隐私说明和限流。

### 3. 构建过程会忽略 TypeScript 和 ESLint 错误

当前配置明确允许生产构建跳过错误：

- `ignoreBuildErrors: true`
- `ignoreDuringBuilds: true`

这可能让有类型错误或明显代码问题的版本进入生产环境。

证据：[next.config.js](/www/wwwroot/petboxnest/apps/storefront/next.config.js:21)

同时，`lint` 脚本仍使用 `next lint`，与当前 Next.js 版本的检查流程不匹配。

证据：[package.json](/www/wwwroot/petboxnest/apps/storefront/package.json:10)

### 4. 全站公开页面失去静态缓存能力  【已经修复】

主布局每次都会读取：

- 当前客户
- 当前购物车
- 配送选项

证据：[主布局](/www/wwwroot/petboxnest/apps/storefront/src/app/[countryCode]/(main)/layout.tsx:17)

实测首页响应头：

```text
Cache-Control: no-store, must-revalidate
```

本机开发环境热请求结果：

- 首页：首字节约 2.3 秒
- 商品列表：约 5.7 秒
- 商品详情：约 8.5 秒
- FAQ：约 30 秒

开发模式数据不能代表生产 Core Web Vitals，但说明公开内容页被会话数据动态化，且多后端请求串联带来了明显等待。

建议：把账户和购物车状态移入独立 Suspense 边界；首页、政策页、文章页和 FAQ 不应因为购物车而整体动态渲染。

### 5. 图片优化被全局关闭  【已经修复】

配置使用：

```js
images: {
  unoptimized: true
}
```

证据：[next.config.js](/www/wwwroot/petboxnest/apps/storefront/next.config.js:27)

而首页现有图片包括：

- `shop-dogs.png`：约 2.25 MB
- `petboxnest-hero.png`：约 2.11 MB
- `shop-cats.png`：约 1.98 MB

这对移动端 LCP 和流量消耗影响很大。

建议：恢复 Next Image 优化，生成 WebP/AVIF，提供响应式尺寸，并将移动端图片尽量控制在 300–500 KB 内。

## 商品与内容问题

### 6. 品牌定位与实际商品严重不一致

首页表达的是猫狗家居用品品牌，但当前商品几乎是羽毛球：

- `12 PACK Badminton Shuttlecocks`
- 羽毛球商品图和详情文案
- 一些旧组件仍保留 `Club Recess`、羽毛球制作过程等内容

这会明显破坏信任和品牌一致性。

遗留证据包括：

- `badminton-desc`
- `contact-hero` 中的 badminton 文案
- `join-the-club` 中的 Club Recess 图片说明

即使部分组件当前未被页面引用，也属于需要清理的旧业务遗留。

### 7. CMS 仍存在测试文章

文章列表出现：

- 测试3
- 测序2
- 测试1
- Pickleball 测试描述

这与美国宠物家居品牌完全不匹配，首页文章区也同步显示这些测试标题。

![文章页测试内容](/tmp/pbn-storefront-audit/08-articles-desktop.png)

建议：清理测试数据，至少准备 6–10 篇正式宠物居家内容，再开放 Journal 入口。

### 8. 商品详情和关联商品存在空白图片

商品详情移动端出现两个大面积空白媒体卡；关联商品图片也显示为空白，只剩商品名称和价格。

![商品详情移动端](/tmp/pbn-storefront-audit/04-product-mobile.png)

关联商品逻辑还是 starter 默认策略，并带有待修改注释：

[related-products/index.tsx](/www/wwwroot/petboxnest/apps/storefront/src/modules/products/components/related-products/index.tsx:21)

建议：

- 修复 Payload 媒体数据或图片 URL。
- 确保关联商品查询返回 thumbnail/images。
- 没有图片时显示符合品牌的明确占位状态。
- 定义真正的关联策略：同宠物、同用途、同空间或互补商品。

### 9. 商品列表发现能力不足

当前 Store 页面存在以下问题：

- 没有商品搜索。
- 排序器像普通项目符号列表。
- 缺少价格、类别、宠物类型、库存等核心筛选。
- 商品区过窄，右侧出现大量无效空白。
- 两件商品不足以验证分页和复杂筛选。

![商品列表桌面端](/tmp/pbn-storefront-audit/03-store-desktop.png)

建议优先增加站内搜索，并重新设计桌面筛选栏和移动端筛选抽屉。

### 10. 购物车最大数量写死为 10  【已经修复】

购物车没有读取真实库存，而是固定允许最多 10 件。

证据：[cart item](/www/wwwroot/petboxnest/apps/storefront/src/modules/cart/components/item/index.tsx:43)

这可能造成：

- 有 50 件库存却只能买 10 件。
- 只剩 2 件库存却允许选择 10 件。
- 到结账阶段才暴露库存错误。

## SEO 与获客问题

### 11. 商品结构化数据不完整    【已经修复】

当前只有存在评论时才输出 Product JSON-LD，而且只有：

- 商品名称
- 图片
- aggregateRating

缺少商品富结果需要的重要数据：

- `offers`
- 实际价格
- 币种
- 库存状态
- SKU
- Brand
- 商品 URL

零评论商品完全没有 Product Schema。

证据：[产品结构化数据](/www/wwwroot/petboxnest/apps/storefront/src/app/[countryCode]/(main)/products/[handle]/page.tsx:169)

### 12. Canonical、站点地图和 robots 未完善   【已经修复】

商品页有动态标题和 Open Graph，这是优点，但没有看到商品 canonical。

项目虽有 `next-sitemap.js`，但：

- `package.json` 没有 `next-sitemap` 依赖。
- 没有 `postbuild` 脚本。
- `siteUrl` 仍使用 `NEXT_PUBLIC_VERCEL_URL`。
- 当前 App Router 没有 `sitemap.ts` 或 `robots.ts`。

证据：

- [next-sitemap.js](/www/wwwroot/petboxnest/apps/storefront/next-sitemap.js:1)
- [package.json](/www/wwwroot/petboxnest/apps/storefront/package.json:10)

因此该配置很可能是未启用的遗留文件。

### 13. 页面级 Metadata 覆盖不足  【已经修复】

商品、分类、集合和文章详情有一定动态 Metadata，但大量页面只依赖空的全局 metadata：

- About
- FAQ
- Contact
- Shipping
- Refund
- Privacy
- Terms
- Account
- Warranty
- Articles 列表等

建议为主要获客页面补充唯一 title、description、canonical 和分享图；账户、购物车、结账等页面应设置 `noindex`。

## 体验与可访问性

### 14. Cookie 弹窗严重遮挡移动端核心内容

移动端 Cookie 弹窗占据了约一半屏幕，遮挡：

- 首页 Hero CTA
- 商品购买信息
- 空购物车 CTA
- 登录页首屏内容

![空购物车移动端](/tmp/pbn-storefront-audit/06-cart-mobile.png)

代码使用 `aria-modal="true"`，但只是普通 `div`：

- 没有可见的关闭或稍后处理方式。
- 没有明确的初始焦点。
- 没有焦点锁定和 Escape 行为。
- 接受后没有发现重新打开 Cookie 设置的入口。

证据：[cookie-consent-banner](/www/wwwroot/petboxnest/apps/storefront/src/modules/layout/components/cookie-consent-banner/index.tsx:73)

不能仅凭截图断言完整 WCAG 结果，但这里存在明显的键盘和阅读顺序风险。

### 15. 无购物车进入 Checkout 直接返回 404

直接访问 `/us/checkout?step=address` 返回 404。阻止无效结账是合理的，但 404 对购物用户不够友好。

建议跳转至购物车并提示：

> Your cart is empty. Add a product before checking out.

同时保留真正不存在页面的 404 语义。

### 16. 缺少完整购物路径自动化测试

目前 Storefront 只有 3 个 Playwright 测试：

- 产品评论
- Admin 礼品卡翻译
- Admin 商店信用翻译

尚未看到以下关键 E2E：

- 首页到商品详情
- 商品选项与加入购物车
- 数量和库存
- 优惠码
- 地址
- 配送方式
- Stripe 支付
- 订单完成
- 登录注册和邮箱验证
- 地址管理
- 密码与邮箱修改
- 移动端导航和 Cart 弹窗
- Cookie consent

## 已做得较好的地方

- 首页已经形成统一的 PetBoxNest 紫色、奶油色、薄荷色视觉体系。
- 首页信息层级清楚，宠物家居品牌感明显。
- 桌面和移动端整体没有发现严重横向溢出。
- 商品轮播有区域语义、按钮标签和 `aria-live`。
- Cart 数量带有 `aria-live`。
- 大多数主要按钮有合理触控高度。
- 商品评论支持审核后展示、筛选、排序和分页。
- Medusa Store API 基本通过项目 SDK 调用。
- TypeScript 检查通过。

![首页桌面端](/tmp/pbn-storefront-audit/01-home-desktop.png)

![首页移动端](/tmp/pbn-storefront-audit/02-home-mobile.png)

## 建议实施顺序

1. 修复或隐藏账户邮箱、密码编辑和邮件订阅假功能。
2. 恢复类型、Lint 和测试作为发布门禁。
3. 拆分全站动态会话请求，恢复公开页面缓存能力。
4. 恢复图片优化并压缩首页大图。
5. 清理羽毛球商品、旧组件和测试文章。
6. 修复商品增强区与关联商品空白图片。
7. 完善 Product Schema、canonical、sitemap、robots 和 noindex。
8. 增加搜索、分类、宠物类型与移动端筛选。
9. 使用真实库存限制购物车数量。
10. 优化 Cookie consent，并补齐完整购物路径 E2E。

## 检查范围与限制

本次检查了首页、商品列表、商品详情、空购物车、账户入口和文章列表的桌面或移动端页面；Checkout 因没有购物车上下文而无法进入实际地址、配送和支付步骤。没有执行生产构建，也没有进行真实付款、真实邮件发送、屏幕阅读器或真实 iOS 设备测试。

完整截图保存在：[/tmp/pbn-storefront-audit](/tmp/pbn-storefront-audit)

Want me to plot this out in Figma with the screenshots and notes?